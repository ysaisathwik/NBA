# Core platform — keep light so the demo runs anywhere.
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6

# Optional: real LLM reasoning. The platform runs fully offline without these.
anthropic>=0.40 ; python_version >= "3.9"
python-dotenv>=1.0

# Optional: Supabase/Postgres long-term store. Falls back to SQLite if not installed.
# supabase>=2.4

# Tests
pytest>=8.0
httpx>=0.27
